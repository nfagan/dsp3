function [fig, labs, spec] = fig_labels_specificity(varargin)

%   FIG_LABELS_SPECIFICITY -- Extract the figure, labels and specificity 
%     from post_plot_func.
%
%     See also dsp3.multi_plot

fig = varargin{1};
labs = varargin{3};
spec = varargin{4};

end